{
  "dependencies": {
    "@babel/core": "^7.18.5",
    "jest": "^28.1.1",
    "jest-environment-jsdom": "^28.1.1",
    "live-server": "^1.2.2"
  },
  "scripts": {
    "test": "jest src --watchAll",
    "serve": "live-server src --ignore src/dist",
    "dev": "tsc -w"
  },
  "devDependencies": {
    "@babel/preset-env": "^7.18.2",
    "@types/jest": "^29.2.0",
    "babel-jest": "^28.1.1",
    "typescript": "^4.8.4"
  },
  "jest": {
    "testEnvironment": "jsdom",
    "setupFiles": [
      "./src/tests-setup.js"
    ]
  }
}
