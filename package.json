{
  "name": "the-zone-ts",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "tsc -w",
    "build": "tsc",
    "test": "vitest src",
    "preview": "live-server src"
  },
  "devDependencies": {
    "live-server": "^1.2.2",
    "typescript": "^4.6.4",
    "jsdom": "^20.0.1",
    "vitest": "^0.24.3"
  }
}
